import "./style.scss";

const Spinner = () => {
  return (
    <div className="spinner__container">
      <span className="spinner" title="Loading, please wait..." />
    </div>
  );
};

export default Spinner;
